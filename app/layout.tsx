import type React from "react"
import type { Metadata } from "next"
import { GeistSans } from "geist/font/sans"
import { GeistMono } from "geist/font/mono"
import { Analytics } from "@vercel/analytics/next"
import { Suspense } from "react"
import "./globals.css"

export const metadata: Metadata = {
  title: "Ismail Hossen - Full-Stack Development & AI Solutions",
  description:
    "Professional freelancing team led by Ismail Hossen. Scalable web, mobile, and AI solutions with React/Next.js, Node/Django, and AI automation.",
  generator: "Next.js",
  metadataBase: new URL("https://ismailhossen.dev"),
  alternates: {
    canonical: "/",
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable}`}>
        <Suspense fallback={null}>{children}</Suspense>
        <Analytics />
      </body>
    </html>
  )
}
